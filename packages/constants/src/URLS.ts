const APP_START_URL = '/'

const APP_HOME_URL = '/home'

const APP_LOGIN_URL = '/login'

const APP_EMPLOYEE_URL = '/employee'

const APP_SCHEDULE_URL = '/schedule'

export const URLS = {
  APP_START: APP_START_URL,
  APP_HOME: APP_HOME_URL,
  APP_LOGIN: APP_LOGIN_URL,
  APP_SCHEDULE: APP_SCHEDULE_URL,
  APP_EMPLOYEE: APP_EMPLOYEE_URL,
}
